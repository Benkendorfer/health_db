name: Checks

on:
  push:
  pull_request:

jobs:
  schema-docs:
    name: Schema docs in sync
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - run: python src/check_schema_docs.py
